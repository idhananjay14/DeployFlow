apiVersion: v1
kind: Service

metadata:
  name: deployflow-frontend
  namespace: deployflow

spec:
  type: NodePort

  selector:
    app: deployflow-frontend

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
