apiVersion: v1
kind: Service

metadata:
  name: deployflow-frontend

spec:
  type: NodePort

  selector:
    app: deployflow-frontend

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
